name: CD Pipeline

on:
  push:
    branches: [main]
    tags: ["v*"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1


      - name: Debug ECR_REGISTRY value
        run: echo "ECR_REGISTRY is ${{ secrets.ECR_REGISTRY }}"

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1
        
      - name: Build, Tag and Push  Docker image to Amazon ECR
        run: |
          docker build -t customer-churn-api .
          docker tag customer-churn-api:latest ${{ secrets.ECR_REGISTRY }}/customer-churn-api:latest
          docker push ${{ secrets.ECR_REGISTRY }}/customer-churn-api:latest
